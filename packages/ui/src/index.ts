/**
 * @gigai/ui — shared design system (extract from apps incrementally).
 *
 * Phase 1: utilities only
 * Phase 2: Button, Card, Badge from worker app
 * Phase 3: full shadcn primitives
 */
export { cn } from "./utils";
